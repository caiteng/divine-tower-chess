# Art Reference Notes

Expected source image:
- `unit_star_progression.png`

Current status:
- `unit_star_progression.png` is intentionally not committed in this repo flow because page Codex merge currently rejects binary files.
- Track `unit_star_progression.placeholder.txt` in git, then add the real PNG from your local git client.
